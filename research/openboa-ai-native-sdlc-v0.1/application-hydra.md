# Application Exercise: Hydra Research Goal

This exercise applies the candidate lifecycle to the research goal recorded in [Hydra Issue #3](https://github.com/openboa-ai/hydra/issues/3). It is deliberately bounded to Hydra; no Ouroboros mutation is required.

## Goal record

| Field | Observed value |
| --- | --- |
| Goal | Establish the evidence base and draft model for OpenBoa AI-Native SDLC v0.1 |
| Human owner | SonSangjoon |
| Repository | openboa-ai/hydra |
| Risk lane | human-gate |
| Current Issue state | status:in-progress |
| Allowed changes | Research artifacts, validator, and tests; no manifest or managed-contract rename |
| Required observation | Validator, unit suite, git diff, and review packet |

## Frame and plan

The goal was admitted before repository mutation because it is long-running, public, and affects doctrine and authority. The plan fixed:

- a 30–40 source first-party corpus;
- source grades and evidence fields;
- frontier-lab and AI-native-company cohorts;
- cross-source lifecycle, risk, and decision matrices;
- an application exercise;
- human approval before public rename.

## Context package

The run loaded:

- workspace and repository AGENTS.md contracts;
- OpenBoa doctrine, operating model, workflow, governance, and GitHub references;
- existing Hydra manifest, marketplace, validator, tests, and README;
- official primary research sources recorded in sources.csv.

The context gap discovered during the exercise is that the current plugin name and skill identity still say openboa-operations, while the draft model proposes openboa-ai-native-sdlc. That is intentionally preserved as a human-gate migration decision.

## Execution evidence

Implementation occurs in an isolated worktree:

- branch: codex/openboa-ai-native-sdlc-research;
- base: dd712df governance: make codex github connector the default;
- main checkout remained clean;
- baseline Hydra validation passed;
- baseline unittest suite passed with 4 tests.

The research package adds:

- source ledger with 40 records;
- evidence synthesis;
- lifecycle matrix;
- risk and authority candidate model;
- decision traceability;
- draft model;
- this application record;
- unresolved questions;
- a validator for the research package.

## Verification plan

The package must pass:

1. source count and required-field validation;
2. duplicate citation and grade validation;
3. Hydra repository validator;
4. full unittest suite;
5. git diff check;
6. manual review of the human-gate boundary and source-gap register.

## Observation and next action

The research goal is not complete until the evidence package is reviewed and the human owner decides:

- whether the working definition is accepted;
- whether the risk-lane refinements should be added;
- whether the public identity should migrate to openboa-ai-native-sdlc;
- whether a compatibility or migration path is required;
- which local product task should be used for a second validation exercise.

Until those decisions are recorded, the current plugin and managed contract remain unchanged.
